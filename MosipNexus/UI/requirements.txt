# Nexus UI — Node.js dependencies
#
# Source of truth: package.json
# Exact versions:   package-lock.json  (use `npm ci` for reproducible installs)
#
# Install (development):
#   npm install
#
# Install (CI / Docker — preferred):
#   npm ci
#
# Requires Node.js >= 20 (Node 22 recommended; see Dockerfile).
#
# ── Runtime (production) ─────────────────────────────────────────────────────
react@^19.1.0
react-dom@^19.1.0
react-markdown@^10.1.0
react-router-dom@^7.6.2
remark-gfm@^4.0.1

# ── Development / build ──────────────────────────────────────────────────────
@types/react@^19.1.8
@types/react-dom@^19.1.6
@vitejs/plugin-react@^4.5.2
typescript@~5.8.3
vite@^6.3.5
