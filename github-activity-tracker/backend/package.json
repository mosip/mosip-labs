{
  "name": "github-activity-backend",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "migrate": "node migrations/runMigrations.js",
    "build": "rm -rf dist && mkdir -p dist && cp -r app.js package*.json migrations routes config services utils db dist"
  },
  "dependencies": {
    "aws-serverless-express": "^3.4.0",
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "dayjs": "^1.11.20",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "luxon": "^3.4.4",
    "nodemon": "^3.1.14",
    "pg": "^8.11.3"
  }
}
